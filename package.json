{
  "name": "node-red-contrib-builder",
  "version": "0.0.12",
  "description": "This is a NodeRed Node that is able to inject a complete msg using json input object rather than having to specify it field by field using inject node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node $NODE_OPTIONS node_module/node-red/red.js -v $FLOWS" 
  },
  "author": "Aurelien Bruno",
  "license": "Apache 2.0",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/abruno06/node-red-contrib-builder.git"
  },
  "node-red":{
    "version": ">=1.1.0",
    "nodes":{
      "translate":"builder/builder.js"
    }
  }  
}
